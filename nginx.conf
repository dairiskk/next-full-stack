# Nginx configuration for Next.js app reverse proxy
# Place this file in /etc/nginx/sites-available/ and symlink to /etc/nginx/sites-enabled/

upstream next_full_stack {
    server localhost:3001;
    keepalive 64;
}

server {
    listen 80;
    server_name example.com www.example.com;
    
    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://next_full_stack;
        proxy_http_version 1.1;
        
        # Headers
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location /_next/static {
        proxy_cache STATIC;
        proxy_pass http://next_full_stack;
        add_header Cache-Control "public, max-age=3600, immutable";
    }

    location /static {
        proxy_cache STATIC;
        proxy_pass http://next_full_stack;
        add_header Cache-Control "public, max-age=3600, immutable";
    }
}

# SSL configuration (uncomment and configure after obtaining certificate)
# server {
#     listen 443 ssl http2;
#     server_name example.com www.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;
#
#     location / {
#         proxy_pass http://next_full_stack;
#         # ... (same headers as above)
#     }
# }
